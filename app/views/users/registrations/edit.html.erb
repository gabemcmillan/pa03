<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
	<%= f.input :firstname, :required => true, :autofocus => true, label: "First Name" %>
	<%= f.input :lastname, :required => true, :autofocus => true, label: "Last Name" %>
	<%= f.input :username, :required => true, :autofocus => true, label: "Username" %>
	<%= f.input :email, :required => true, :autofocus => true %>
    <%= f.input :zip, :required => true, :autofocus => true %>
	

    <%= f.input :password, :autocomplete => "off", :hint => "leave it blank if you don't want to change it", :required => false %>
    <%= f.input :password_confirmation, :required => false %>
    <%= f.input :current_password, :hint => "we need your current password to confirm your changes", :required => true %>
  </div>

  <div class="actions">
	<%= link_to 'Back', :back, :class => 'btn' %>
    <%= f.button :submit, "Update", class: "btn btn-success" %>
  </div>
<% end %>






	
	