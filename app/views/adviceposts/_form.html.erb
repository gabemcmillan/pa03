
<script type="text/javascript">
<!--
$('#advicepost_miniresume').live('keyup keydown', function(e) {
  var maxLen = 2000;
  var left = maxLen - $(this).val().length;
  $('#char-count').html(left);
}); 
//-->
</script>

<%= form_for(@advicepost) do |f| %>
  	
  <div class="field">
    <%= f.hidden_field :advisor_id %>
  </div>

  <div class="field">
    <%= f.hidden_field :status %>
  </div>

  <div class="field">
	<strong><%= f.label "Headline *" %></strong>
	    <span class="help-block">Enter a headline of your expertise.</span>
    <%= f.text_field :headline, :required => true %>
  </div>


  <div class="field">
    <strong><%= f.label "Category *" %></strong>
    <%= f.select :category_id, Category.order("categoryname").all.collect {|category| [category.categoryname,category.id]}, :include_blank => 'Please select a category...', :required => true %>
  </div>

  <div class="field">
    <strong><%= f.label 'Mini-Resume *' %></strong>
    <span class="help-block">(2000 character max.) Summarize your experience and accomplishments. Be specific. Show off. Add any relevant links as well.  Enter years experience and a descriptive title.</span>
    <%= f.text_area :miniresume, :required => true %>
  </div>
  <div>Characters left: <span id="char-count">2000</span></div>
  </br>


  <div class="field">
	<strong><%= f.label "Website or Blog URL" %></strong>
    <%= f.text_field :website %>
  </div>

  <div class="field">
	<strong><%= f.label "Twitter profile URL" %></strong>
    <%= f.text_field :twitter %>
  </div>

  <div class="field">
	<strong><%= f.label "LinkedIn profile URL" %></strong>
    <%= f.text_field :linkedin %>
  </div>



  <br />

<div class="field">
<%= f.fields_for :rating, @rating do |builder| %>
	<fieldset>
  <div class="field">
    <%= builder.hidden_field :advisor_id %>
  </div>
  <div class="field">
    <%= builder.hidden_field :score %>
  </div>
  <div class="field">
    <%= builder.hidden_field :advicepost_id %>
  </div>
	<fieldset>
<% end %>
</div>



By clicking update you verify all the information you have entered for this Advice Listing is a fact.  If information entered is found false you may, at a minimum, be removed from the site. <br /><br />


  <div class="actions">
	<%= link_to 'Back', :back, :class => 'btn' %>
    <%= f.submit 'Update', :class => 'btn', class: "btn btn-success" %>

  </div>
  
<% end %>






