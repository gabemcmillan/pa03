<%= form_for(@getadvice) do |f| %>
  <% if @getadvice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@getadvice.errors.count, "error") %> prohibited this getadvice from being saved:</h2>

      <ul>
      <% @getadvice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
